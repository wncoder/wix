<UserControl x:Class="Microsoft.Tools.WindowsInstallerXml.UX.TitleControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="450">
    <Control.Resources>
        <ResourceDictionary Source="Styles.xaml" />
    </Control.Resources>

    <Grid Style="{StaticResource TitleStyle}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition />
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding Path=Title}" Style="{StaticResource TitleTextStyle}"/>
        <Button Command="{Binding Path=CloseCommand}" Style="{StaticResource TitleCloseButtonStyle}" Grid.Column="1"/>
    </Grid>
</UserControl>
